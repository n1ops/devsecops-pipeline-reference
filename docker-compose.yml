services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=true
      - DATABASE_URL=sqlite:///./tasks.db
    volumes:
      - ./app:/app/app
    restart: unless-stopped
